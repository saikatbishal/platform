/** Two-cell corner nameplate: product name, then build. */
export interface VersionBadgeProps {
  name?: string;
  version?: string;
}
export declare function VersionBadge(props?: VersionBadgeProps): JSX.Element;
