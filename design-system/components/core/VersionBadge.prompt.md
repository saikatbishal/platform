Top-left chrome over a full-bleed surface (the map). One per screen.

```jsx
<VersionBadge name="Platform" version="v0.1" />
```

Ink on `--surface` with a hairline, never accent — the accent is reserved for travelled route. Radii nest concentrically: outer 2px, border 1px, inner 1px.
