The station board folded down to a control. Use it for the one way back into a dismissed board — not as a general button.

```jsx
<BoardChip code="PF" onClick={raiseBoard}>Sign in</BoardChip>
```

Board paint does not follow the theme, so this chip looks identical in light and dark. Hover lifts it 1px; there is no colour change, because the paint is the point.
