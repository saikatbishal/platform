Sits directly above a `StationBoard` so the board reads as hung rather than pasted on.

```jsx
<BoardBracket />
<StationBoard latin="Platform" code="PF" />
```

It stretches to its container's width. Nothing else in the system uses `--board-frame`.
